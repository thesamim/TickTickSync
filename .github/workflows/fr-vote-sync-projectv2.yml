name: FR Vote Sync (ProjectV2, no npm)

on:
  schedule:
    - cron: '*/15 * * * *' # every 15 minutes
  workflow_dispatch:

permissions:
  issues: write
  contents: read
  projects: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Run FR vote sync (ProjectV2)
        uses: actions/github-script@v6
        with:
          script: |
            // Replace the following with your script content
            const OWNER = process.env.OWNER || context.repo.owner;
            const REPO = process.env.REPO || context.repo.repo;
            const PROJECT_NUMBER = Number(process.env.PROJECT_NUMBER || '2');
            // ... rest of your script here ...
        # optional env override
        # env:
        #   OWNER: your-owner
        #   REPO: your-repo
        #   PROJECT_NUMBER: '2'
